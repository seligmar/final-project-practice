import React from 'react'

const baseURL = 'http://localhost:3001/'
cosnt newUserURL = baseURL + 'new-supporter'
const logInURL = baseURL + 'signin'

const API = () => {
  return <div />
}

export default API
